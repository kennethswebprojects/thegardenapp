extends layout

block content
  h1= title
  p Welcome to #{title}
  p This is the Home Page
  
    
    style.
      body        { padding-top:80px; }
    .container
      .jumbotron.text-center
        h1
          span.fa.fa-lock
          |  Node Authentication
        p Login or Signup with:
        a.btn.btn-default(href='/login')
          span.fa.fa-user
          |  Local Login
        a.btn.btn-default(href='/signup')
          span.fa.fa-user
          |  Local Signup

